<html>
  <body style="width: 550px; margin: 10px auto; font-family:Helvetica Neue,sans-serif">
    <a href="http://backtracks.co" style="text-decoration: none;">
      <center>
        <%= image_tag 'http://backtracks.co/public/images/backtracks-small.png', style: 'display: inline; height: 50px; vertical-align: middle;' %>
        <h1 style="display: inline; color: #1EAEDB; height: 50px; font-size: 32px; vertical-align: middle;">Backtracks</h1>
      </center>
    </a>
    <div style="width:100%; padding-top: 20px; padding-bottom: 20px">
      Hi everyone! Hopefully everything looks pretty similar, but
      the email you're looking at is coming from the 2016 edition
      of Backtracks, and the hope is that it'll be more reliable
      and easier to investigate any issues if they pop up. If
      anything seems weird, please let me know ASAP so I can look
      into it! Thanks a lot for your patience; I'm really excited
      to bring you some new features soon now that the foundation
      is more solid.<br/>
      Questions? Comments? Wanna say hello? Just hit reply!
      <br/><br/>
      -your pal @ckolderup
    </div>
    <% years.each do |year| %>
      <% next if year[:album_rows].empty? %>
      <h2>
        <%= "#{Time.now.strftime('%B')} #{Time.now.year - year[:num]}" %>
      </h2>
      <table style="padding: 25px 28px;">
        <th colspan="3">
          <h3>Albums</h3>
        </th>
        <% year[:album_rows].each do |row| %>
          <tr>
            <% row[:albums].each do |album| %>
              <% album_text = "#{album.artist} - #{album.title}" %>
              <td>
                <a href="<%= album.url %>">
                  <%= image_tag album.cover, alt: album_text, height: 160, title: album_text, width: 160 %>
                </a>
              </td>
            <% end %>
          </tr>
        <% end %>
      </table>
      <table style="width: 550px;">
        <tr>
          <th>
            <h3>Artists</h3>
          </th>
          <th>
            <h3>Tracks</h3>
          </th>
        </tr>
        <tr>
          <td style="vertical-align: top;">
            <% if year[:artists].empty? %>
              <center>
                <small>No Data</small>
              </center>
            <% else %>
              <ol style="list-style-position: inside; width: 195px; padding: 0px;">
                <% year[:artists].each do |artist| %>
                  <li style="text-overflow: ellipsis; overflow: hidden; white-space: nowrap;">
                    <a title="<%= artist.name %>" style="color: #1EAEDB; text-decoration: none;" href="<%= artist.url %>">
                      <%= artist.name %>
                    </a>
                  </li>
                <% end %>
              </ol>
            <% end %>
          </td>
          <td style="vertical-align: top;">
            <% if year[:tracks].empty? %>
              <center>
                <small>No Data</small>
              </center>
            <% else %>
              <ol style="list-style-position: inside; width: 345px; padding: 0px;">
                <% year[:tracks].each do |track| %>
                  <% track_title = "#{track.artist} - #{track.title}" %>
                  <li style="text-overflow: ellipsis; overflow: hidden; white-space: nowrap;">
                    <a title="<%= track_title %>" style="color: #1EAEDB; text-decoration: none;" href="<%= track.url %>">
                      <%= track_title %>
                    </a>
                  </li>
                <% end %>
              </ol>
            <% end %>
          </td>
        </tr>
      </table>
      <hr/>
    <% end %>
    <div style="font-size: 10px">
      You can unsubscribe from your <a href="http://backtracks.co/account">Account</a> page.
      Feedback? Just reply to this email.
    </div>
  </body>
</html>
